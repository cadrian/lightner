#!/bin/bash

mkdir -p "$HOME/.lightner.d"
cd "$HOME/.lightner.d"

exec >> log
date -R
exec java \
     -Dlightner.data.driver=net.cadrian.lightner.dao.sqlite.SqliteDriver \
     -classpath /usr/share/maven-repo/net/cadrian/lightner/main/debian/*:%CLASSPATH% net.cadrian.lightner.Main
